{
  "name": "lilili",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:lan": "next dev -H 0.0.0.0",
    "dev:clean": "rm -rf .next && next dev -H 0.0.0.0",
    "build": "next build",
    "build:app": "mv app/api api-bak; BUILD_TARGET=app next build; mv api-bak app/api",
    "sync:ios": "npm run build:app && npx cap sync ios",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "14.2.35"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.35"
  }
}
