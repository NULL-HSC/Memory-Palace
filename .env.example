# 后端地址 —— 联调时填入队友的腾讯云公网地址（hackathon-plan §4.2）
# 不填 = 全 mock 模式，前端可独立开发演示

BACKEND_URL=http://公网IP:8000                 # 服务端代理用（浏览器走 /api/* 同源转发）
NEXT_PUBLIC_BACKEND_URL=http://公网IP:8000     # iOS App 直连用（CapacitorHttp 原生请求）
